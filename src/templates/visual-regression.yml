# Generated by `looksee init` — see https://github.com/luisgrandegg/looksee
# Safe to edit; re-running `looksee init` will detect this file and prompt before overwriting.
name: Visual Regression
on:
  pull_request:
  push:
    branches: [main]

jobs:
  visual:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - uses: luisgrandegg/looksee@<action-version>
